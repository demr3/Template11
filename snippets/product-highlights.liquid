<!-- image-content.liquid -->
<style>
    .gallery {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        justify-content: center;
        margin: auto;
    }
    
    .image-container {
        position: relative;
        width: calc(33.33% - 10px); /* Three images in a row */
        aspect-ratio: 1 / 1; /* Makes all images square */
        overflow: hidden;
    }
    
    .image-container img {
        width: 100%;
        height: 100%;
        object-fit: cover; /* Ensures the image fits without distortion */
        transition: transform 0.3s ease-in-out;
    }
    
    .image-container:hover img {
        transform: scale(1.1); /* Slight zoom effect on hover */
    }
    
    .overlay {
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.7);
        color: white;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 15px;
        opacity: 0;
        transition: opacity 0.4s ease-in-out;
    }

    .overlay h2 {
        color: white;

    }
    
    .image-container:hover .overlay {
        opacity: 1;
    }
    
    /* Responsive for smaller screens */
    @media (max-width: 768px) {
        .gallery {
            flex-direction: column;
            gap: 15px;
        }
        
        .image-container {
            width: 100%;
            aspect-ratio: unset; /* Let images adjust naturally */
            height: 400px; /* Keeps consistent height */
        }

        .image-container .overlay {
            opacity: 1; /* Keep overlay visible on mobile */
        }
    
        .image-container:hover .overlay {
            opacity: 0; /* Disable hover effect on mobile */
        }
    }
</style>
<div class="gallery">
    <div class="image-container">
        <img src="{{ block.settings.image1 | img_url: '600x' }}" alt="Vacuum Compression">
        <div class="overlay">
            <h2>Effortless Compression</h2>
            <p>The Airless backpack features a built-in vacuum system to maximize packing space.</p>
        </div>
    </div>

    <div class="image-container">
        <img src="{{ block.settings.image2 | img_url: '600x' }}" alt="Quick Access Pocket">
        <div class="overlay">
            <h2>Quick Access Pocket</h2>
            <p>Secure your essentials with an easy-access pocket for cards and small valuables.</p>
        </div>
    </div>

    <div class="image-container">
        <img src="{{ block.settings.image3 | img_url: '600x' }}" alt="Smart Packing Design">
        <div class="overlay">
            <h2>Smart Packing Design</h2>
            <p>Keep your clothes wrinkle-free with the integrated vacuum-seal technology.</p>
        </div>
    </div>
</div>